Description:
    Explain the generator

Example:
    rails generate tfg_scaffold Thing

    This will create:
        what/will/it/create

    You can test this out by running `rails g tfg_scaffold lib/generators/tfg_scaffold/driver.yaml`